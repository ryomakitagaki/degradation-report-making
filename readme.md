オプション	説明
--input	入力画像フォルダ
--output	出力フォルダ（デフォルト: dataset）
--classes	クラス名リスト
--instruction	Geminiへのアノテーション指示
--model	Geminiモデル（デフォルト: gemini-2.0-flash）
--split	train / val / test
--dry-run	動作確認のみ（APIを呼ばない）
--api-key Gemini API キーを入れる これはgithubで共有すると誰かに不正利用されるので毎回いれる